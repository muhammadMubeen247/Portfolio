# Base URL of the deployed FastAPI backend (FastAPI Cloud), no trailing slash.
# Leave unset for local dev; Vite's dev proxy (vite.config.js) forwards /about to localhost:8000.
VITE_API_BASE_URL=https://your-app.fastapicloud.dev
